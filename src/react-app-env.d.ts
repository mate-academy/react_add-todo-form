/// <reference types="react-scripts" />

interface Todo {
  id: number,
  title: string,
  completed: boolean,
  userId: number,
}

interface Users {
  id: number,
  name: string,
  username: string,
  email: string,
}
